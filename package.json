{
  "scripts": {
    "install": "lerna link",
    "prepare": "lerna run prepare",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.12",
    "@types/node": "^11.13.9",
    "jest": "^24.8.0",
    "lerna": "^3.13.4",
    "ts-jest": "^24.0.2",
    "typescript": "^3.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "collectCoverage": true,
    "collectCoverageFrom": [
      "**/src/main/**/*.{js,ts}"
    ],
    "moduleNameMapper": {
      "^@chaff/fui/([a-z]+)$": "<rootDir>/packages/fui/$1",
      "^@chaff/fui-([a-z]+)$": "<rootDir>/packages/fui-$1/"
    },
    "globals": {
      "ts-jest": {
        "diagnostics": {
          "ignoreCodes": [
            "TS151001"
          ]
        }
      }
    }
  },
  "license": "MIT",
  "private": true
}
