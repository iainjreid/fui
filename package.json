{
  "name": "fui",
  "scripts": {
    "install": "lerna link",
    "prepare": "lerna run prepare",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^11.13.10",
    "jest": "^24.8.0",
    "lerna": "^3.16.1",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "jest": {
    "preset": "ts-jest",
    "collectCoverage": true,
    "collectCoverageFrom": [
      "**/src/main/**/*.{js,ts}"
    ],
    "moduleNameMapper": {
      "^@chaff/fui/([a-z]+)$": "<rootDir>/packages/fui/$1",
      "^@chaff/fui-([a-z]+)$": "<rootDir>/packages/fui-$1/"
    },
    "globals": {
      "ts-jest": {
        "diagnostics": {
          "ignoreCodes": [
            "TS151001"
          ]
        }
      }
    }
  },
  "license": "MIT",
  "private": true
}
